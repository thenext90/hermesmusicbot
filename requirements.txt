# HermesMusicBot — dependencias Python
# Instalar:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0

# yt-dlp también se usa como binario desde el sistema; esta copia garantiza
# una versión reciente dentro del contenedor (se puede actualizar con pip).
yt-dlp>=2025.1.1

# Adaptador opcional de Telegram (comentar si solo se usa la API HTTP)
python-telegram-bot>=21.0

# Cliente HTTP para las pruebas de humo (tests/smoke_test.py)
httpx>=0.27
